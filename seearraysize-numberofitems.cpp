//Normally, when you code your program so it showws the number of items in array, it displays the number of items in the array in bytes.
//This is an example of how to display the number of items in an array:
#include <iostream>
using namespace std;
int main() {
  int items[3] = { "1", "2", "3" }; //create an array with the name "items"
int getArrayLength = sizeof(items) / sizeof(int)
}
